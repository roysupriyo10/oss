import { type FigmaPluginConfig } from "@/types";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const build = async function (_config: FigmaPluginConfig): Promise<void> {
  throw new Error(
    "Figma plugin build is not yet implemented. Contributions welcome!",
  );
};

export default build;
